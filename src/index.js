import React from 'react';
import ReactDOM from 'react-dom';
import AudioPlayer from './components/AudioPlayer';

const rootElement = document.getElementById('root');

ReactDOM.render(<AudioPlayer />, rootElement);